<%= partial "/partials/sidenav" %>
<article class="home-content">
	<section class="portfolio-grid">
		<% data.works.each do |work| %>
			<% link_to "/work/#{work.permalink}" do %>
				<figure class="grid-item type-<%= work.type %>">
					<%= work.title %>
				</figure>
			<% end %>
		<% end %>
	</section>
</article>
